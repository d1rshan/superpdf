# 07: Visual design pass

**What to build:** The minimalist design applied across the whole app: dark near-black + neon green theme with a light (white + neon green) toggle, following the minimalist-ui skill. Applied to both views, cards, sections, drawer, and viewer — consistent spacing, typography, and status badges.

**Blocked by:** 05, 06.

**Status:** done

- [x] Dark theme (near-black + neon green) applied across all views
- [x] Light mode toggle (white + neon green) works everywhere
- [x] Design follows the minimalist-ui skill (no gradients, heavy shadows, or generic AI patterns)
- [x] `lint` and `typecheck` pass; committed in relevant checkpoints with conventional commit messages (multiple commits as work progresses, not one lump commit)

Note: light-mode accent is `#00b85c` (same hue, deepened for AA contrast on white) — the `ponytail:` comment in `globals.css` documents the ceiling.
